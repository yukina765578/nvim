return {
  "akinsho/nvim-bufferline.lua",
  dependencies = { "nvim-tree/nvim-web-devicons" },
  version = "*", -- Let's use the latest version on each install
  opts = {
    options = {
      mode = "tabs",
      seperator_style = "slant",
    }
  }
}
